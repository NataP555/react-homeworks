const home = '/';
const movies = '/movies';
const details ='/movies/:id';
const register ='/register';
const login = '/login';
const routes ={
    home,
    movies,
    details,
    register,
    login,
}

export default routes